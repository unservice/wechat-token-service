{
  "name": "@unservice/wechat-token-service",
  "version": "0.0.1",
  "module": "lib/client.js",
  "types": "lib/client.d.ts",
  "files": [
    "/lib"
  ],
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "test": "bun test --coverage",
    "build": "rm -rf ./lib && bun build ./src/client.ts --outdir ./lib --target bun"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.2.2",
    "@unservice/wechat-sdk": "^1.3.0",
    "hono": "^4.4.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.3.1"
  }
}
